<!DOCTYPE html>

<title>Multimedia</title>

{% block body %}
    <h1>Multimedia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ multimedia.id }}</td>
            </tr>
            <tr>
                <th>IdArt</th>
                <td>{{ multimedia.idArt }}</td>
            </tr>
            <tr>
                <th>Src</th>
                <td>{{ multimedia.src }}</td>
            </tr>
            <tr>
                <th>Home</th>
                <td>{{ multimedia.home ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ multimedia.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('articulos_multimedia_index') }}">back to list</a>

    <a href="{{ path('articulos_multimedia_edit', {'id': multimedia.id}) }}">edit</a>

    {{ include('articulos/multimedia/_delete_form.html.twig') }}
{% endblock %}
